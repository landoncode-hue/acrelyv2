"use client";

/**
 * Background service to check for overdue leads
 * Runs every 5 minutes when dashboard is open
 * Creates notifications for agents with overdue follow-ups
 */
export function useOverdueLeadChecker() {
    return null;
}
