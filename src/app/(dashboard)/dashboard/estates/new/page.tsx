import { CreateEstateClient } from "./create-estate-client";

export const metadata = {
    title: "Create New Estate | Acrely",
};

export default function CreateEstatePage() {
    return <CreateEstateClient />;
}
