/**
 * Convenience barrel export for all fixtures.
 *
 * Usage in spec files:
 *   import { test, expect } from '@tests/fixtures';
 */
export { test, expect } from './auth.fixtures';
export { TEST_DATA } from './data.fixtures';
export type { TestData } from './data.fixtures';
